#ifndef PARSER_H
#define PARSER_H

class Parser
{
private:
public:
    // Recursive descent parser: Should take in a sequence of tokens, and production rules
    static bool Parse();
};

#endif
